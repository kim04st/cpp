#include <iostream>
using namespace std;

class CRect {
    int w, h;
public:
    CRect(int w, int h) : w(w), h(h) {}
    void Area() { cout << w * h << endl; }
};

int main() {
    CRect *p = new CRect(3, 4);
    p->Area();
    delete p;
    return 0;
}
